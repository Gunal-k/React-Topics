import React from 'react';

// This is a class component in React
// It extends React.Component and must have a render method
// The render method returns the JSX to be displayed
// The class name must start with a capital letter
// The class component can have its own state and lifecycle methods
class Greeting extends React.Component {
    render() {
        return <h1>Hello, Gunal from class component</h1>
    }
}

export default Greeting;